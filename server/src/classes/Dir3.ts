export default class Dir3 {
  left: number;
  right: number;
  drive: number;
  constructor(left: number, right: number, drive: number) {
    this.left = left;
    this.right = right;
    this.drive = drive;
  }
}
