{
  "scripts": {
    "start": "live-server --port=5000",
    "build": "rm -r -f build/ && tsc -w"
  },
  "devDependencies": {
    "typescript": "^4.1.5"
  },
  "dependencies": {
    "@types/socket.io": "^2.1.13",
    "socket.io": "^3.1.1"
  }
}
